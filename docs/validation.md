# 验证范围与证据索引

构建成功、反射导入成功和实机验收是三个不同结论。本工具的 `status` 不会自动把运行标为用户已验收；最终状态 `final_ready_pending_user_validation` 明确保留这一边界。

## 历史接受基线与当前范围

| 项目 | 已记录结论 | 适用边界 |
|---|---|---|
| C2M1→C5，历史 18 号成果 | 63 份 HDR 反射重捕获及原名回填；21 项运行检查、27 份登记原输入保持、用户无异常反馈通过 | 当时输入、环境和 HDR 观察范围；不代表新工作流全部重新实机通过 |
| 历史 19 号重复应用审计 | 基础 BSP 和三份模式 LMP 再次应用已有配置均逐字节不变 | 已检查配置的静态幂等性，不是其他地图兼容证明 |
| C6M1→C5，旧 preserve | 0.1.1 原生烘焙、模型缓存迁移和七文件包验证；用户随后采样的 48 张 HDR 已通过文件审计，采样后重连被拒绝 | 只适用于旧天气版本，不可复用于晴天输出；不等于画面或玩法接受 |
| C6M1→C5，0.2.0 replace 接受成果 | 基础/h/l/s 静态天气保护与幂等性、原生 HDR 烘焙、48 张 HDR 反射导入及七文件打包/解包核对通过；用户反馈测试完毕、基本无问题 | 适用于下列 run 和最终包；没有逐模式、全路线或全部画质的测试记录 |
| C2M1→C4，0.4.0 固定阴天 | 原生 HDR 烘焙、63 份新 HDR 反射、八文件包审计及用户原名实机验收通过 | 限运行 `1b6530c3c5454b7c` 的最终包；未新增雨雷或动态风暴，未逐模式/全路线记录 |
| 0.3.0 内置 C5 预设 | 将既有目标参数整理为版本化数据，配置不再要求 C5 BSP/LMP | 预设整理不扩展来源地图或实机验收范围；本版回归与已接受游戏结果分别记录 |

2026-09-13 的新工作流验证记录：127 项合成测试通过；一次全新 C2 原生 HDR 四跳/静态模型烘焙成功，输出 BSP 为 26,590,216 字节。几何、detail prop 布置、VHV 拓扑保护审计通过，官方 VPK 的七个文件（含 addoninfo）打包/解包验证通过，采样交接生成 128 份旧反射别名资源。本次没有启动游戏、安装采样文件或进行新的引擎捕获，因此新管线实机验收仍待执行。

C6 基础 BSP 与三模式 LMP 重复应用均不变；原始 C6 原生布局审计和 48 HDR / 98 别名资源预检通过。初版整理时尚未进行 C6 烘焙或游戏运行；此后的 0.1.1 烘焙证据见下文。27 份登记原输入哈希保持。新 C2 烘焙与历史 16 号对照只见 lump 35、40、53、55 的光照相关差异；这不构成并行烘焙整文件哈希确定性的保证。

0.1.1 的 C6 首次实际构建暴露了旧 BSP 缓存与现行模型资源不一致的问题：7 种模型的 89 份 VHV 非颜色字段不同，74 份仅 checksum 变化，15 份另有网格分段变化。完成模型证据校验修复后，旧输出回放审计及全新原生 HDR 四跳/静态模型构建均通过，89 份缓存逐项匹配当前模型；`sprp` 布置和受保护 BSP 数据保持。官方 VPK 七文件打包与解包核对通过。最终解析器再次审计新输出通过，C2 既有实际烘焙的回归审计通过（无需模型迁移）。此阶段只确认离线构建链；后续用户捕获证据如下。

0.2.0 整理时检查了用户旧 C6 捕获：日志先有正确地图/guard/请求，随后 48/48 样本进度，原生 Host_NewGame 后 VAC 从 insecure 变为 secure，并发生连接拒绝。之后空会话的 guard 拒绝不能证明首次采样失败。实际 BSP 的 48 张采样 HDR 全部改变，非 PAK 数据、原有 PAK 资源及采样坐标保持，RGB 有限；原生非有限 alpha 保留并记录。此前进程的启动命令行没有留存，不能断言缺少 -insecure 是唯一原因。新指南要求核对进程参数，并且不再把重连成功当作文件写入判据。

0.2.0 replace 原生集成构建使用 HDR 四跳/完整静态模型组合，VRAD 约 6 分 23 秒；输出 BSP 为 21,774,364 字节，SHA256 `49e5ea98e8f0bbf09ca718bf0621711f377ba18ab55390b69f9615588068dff2`。七文件 VPK SHA256 `87cc53778bfff91063ca58ee22d77dbb2538787fa925126c023144e10b20a9b8`，官方解包逐字节核对通过。模型资源、登记输入及产物哈希核验通过。该次仅为离线集成构建；后来独立晴天运行的捕获和用户接受见下文。

另以历史16号基线和17号真实捕获/日志回放当前反射导入流程，实际执行官方BSPZIP与VPK，生成BSP的SHA256为 `5541e7d1ef0f2c155c74379db698b061703cab8e54839bf19db78140383993d7`，与已验收18号逐字节相同，七文件包解包核对通过。这验证了导入链的结果保真，不是一次新的引擎采样或C6实机验收。

历史原始日志、完整地图和实验包保留在本机，源码发行只带精选结论，不附个人安装路径、账户标识或游戏文件。历史实机结论不能继承为新生成 VPK 的验收结论。

## 本版可重复检查

0.3.0 预设归档验证（2026-09-13）：内置 JSON 的 57 项视觉字段和 8 项启动曝光输出与 C5 BSP、模式 LMP 参考值一致。预设 SHA256 为 `8b00a0b629caafe70e560f973c69a8165978b8eeaae1119c5ea448687ed8e061`。对相同 C2/C6 基础 BSP 及 h/l/s 输入共 8 组，预设路径与重构前 donor 路径输出逐字节一致，重复应用均不变，非实体数据和受保护玩法 I/O 审计通过。C6 基础输出同时与上述接受运行的烘焙输入快照一致。

使用不含 `reference_bsp` / `reference_lmp` 的全新 C6 配置执行预检通过，24 项目标资源均找到，四份输出与旧路径一致。测试套件共 181 项，180 项通过，1 项因 Windows 无创建符号链接权限而跳过；预设快照保存、哈希漂移拒绝和构建清单保留有合成测试覆盖。wheel 实际安装后在隔离 Python 导入路径中成功加载预设；wheel/sdist 清单只包含指定预设 JSON，不包含游戏资产或本机配置。源码 ZIP 另按发行步骤解包运行测试。这次整理未重新运行 VRAD 或游戏捕获，不把字节等价检查表述为一次新的实机验收。

在源码根目录执行：

```powershell
python -m unittest discover -s tests -v
python -m l4d2_bsp.workflow --help
python -m l4d2_bsp.workflow check --config config.local.json
```

合成测试使用自建最小数据，覆盖容器/实体修改、受限风格、反射、路径、原生审计、打包和交接的正常及拒绝路径，不要求测试仓库携带官方 BSP。具体结果以本次测试输出为准，不能用测试数替代运行报告。

本机集成构建进一步调用真实 VRAD/VPK。它必须同时满足结构保护、模型光照审计、包解出文件一致和输入哈希保持。新流水线的 C2 集成证据应与历史 18 号实机证据分开记录；C6 用户测试不得通过复用 C2/历史捕获来冒充完成。

| 本次运行证据 | 位置或字段 | 回答的问题 |
|---|---|---|
| 配置、输入/工具哈希、状态 | `<run>/run.json` | 实际版本和配置、停在哪一步 |
| 预设身份（新配置） | `<run>/preset.json`、`run.json` 中预设元数据及输入清单 | 固定预设 ID、版本、JSON SHA256；旧 donor 运行继续使用原参考哈希 |
| 原生编译日志 | `<run>/bake/vrad.log` | 缺模型、材质、警告或超时 |
| 保护结果 | `bake_audit`、`preflight` | 允许的风格变动、保护数据是否通过 |
| 天气策略 | `atmosphere_policy`、`preflight.base_style.weather`、各 `mode_styles` | replace/preserve、移除实体/分支、剩余雨实体、天气所有权与玩法保护 |
| 离线包验证 | `offline_package` | 包路径、SHA256、解包字节验证 |
| 本次人工命令 | `<run>/CAPTURE-STEPS.txt` | 当前 run 的实际控制台命令 |
| 原始捕获 | `<run>/evidence/attempt-XX/` | 本次采样 BSP 和引擎日志 |
| 导入过程 | `<run>/reflection-import/attempt-XX/` | 审计后回填及错误证据；失败不会覆盖旧 attempt |
| 最终结果 | `reflection_import`、`final_package` | 采样结构/资源检查及最终包身份 |
| 画面和玩法 | 用户保存的 PNG 与验收说明 | 实际挂载后的视觉与玩法是否可接受 |

本机清单和日志通常含绝对路径，分享前需脱敏。完整游戏资产不属于公开证据附件。

## C6 首次独立验收记录

用户对 C6 晴天结果的原话为：“我C6测试完了，基本上没有什么问题，C5当前你按照一个预设组织工程，可以更新归档，推送到云端仓库了”。这记录为该次结果的用户接受，不推断未说明的模式、路线、事件或画质覆盖。

| 接受成果身份 | 值 |
|---|---|
| 本机运行 | `runs/c6-c5-clear-01`，run ID `49f6ed92538349a1` |
| 来源与天气 | `c6-c5` / `c6m1_riverbank` / `replace`（原 donor 配置） |
| 最终 VPK | `lmc_c6m1_riverbank_49f6ed92538349a1_final.vpk` |
| VPK SHA256 | `89aa0931b13c59d27b2c4d0709591ecda8dcbdc700c0cd23aae2c339068c7bb5` |
| BSP SHA256 | `230eae0f645b64669d28af7153df46e5483e1e6058533a15003d72711423fd6c` |

该运行的 `reflection_import` 记录 48 张 HDR 采样，全部发生字节变化；非 PAK 数据、原有 PAK 内容、采样坐标保持，RGB 有限，LDR 与默认反射保留。`final_package.native_extract_verified` 为真，包含七个文件。引擎日志记录正确 guard 后的唯一请求和原生重载，没有请求前后拒绝。文件审计和用户反馈分别支持结构来源与实际观察结论，不能互相替代。

人工接受另存本机 `USER-VALIDATION.md`；原 `run.json` 保留机器状态 `final_ready_pending_user_validation`，不为表达接受而修改清单或旧配置。公开源码只包含此脱敏结论和哈希，不包含完整 BSP/VPK 或原始个人日志。0.3.0 的预设重构不会重新标记这次旧运行，也不将其描述为一次新的烘焙或采样。

后续运行可另存 `USER-VALIDATION.md`，不要编辑工具清单来表达主观通过。可复制以下模板：

```markdown
# 用户实机记录
- 日期、游戏版本：
- run ID、source_profile/preset（或旧 profile）、atmosphere_policy：
- VPK 文件名及 SHA256：
- 阶段：原版 / offline（旧反射） / final（新 HDR 反射）
- 原地图名、游戏模式：
- 已停用同图 addon、是否完整重启：
- HDR、分辨率、画质、FOV：
- 开场结束和天气状态：
- 自选机位 ID、getpos 输出、PNG 文件：
- 天空/远雾、建筑/地面阴影：
- 车辆/武器反光、室内外曝光：
- replace：无原雨/风暴/闪电/雨声；preserve：原天气表现：
- 新娘 Witch 被惊动后的尸潮是否正常：
- 路线、触发事件、NAV/角色行为：
- 缺失模型/材质、错误日志或异常：
- 结论：通过的具体范围 / 未覆盖 / 需修复
```

至少区分检查通过、离线构建通过、采样文件审计通过、画面接受和玩法路线检查。preserve 的动态天气可能在 guard 后变化；replace 也需要检查外部脚本和实际挂载后的行为。HDR 纹理格式审计不能判断反射是否符合期望氛围。三模式补丁静态处理不代表各模式完成实机测试。

## C2→C4 首次独立验收记录

2026-09-13，用户对原地图名下的最终包反馈：“应该可以了，没什么问题，C4风格化可验收”。记录为本次 C4 固定阴天结果接受，不推断未明确记录的模式、全路线、逐项事件或全部画质覆盖。

| 接受成果身份 | 值 |
|---|---|
| 本机运行 | `runs/c2-c4-static-01`，run ID `1b6530c3c5454b7c` |
| 来源 / 预设 / 策略 | 原始 `c2m1_highway` / `c4m3-overcast-static-v1` / `replace` |
| 预设 SHA256 | `93ae81a35b86f9134acfb89e3829aeb6f2ba47a2c708982da844a78b452aa301` |
| 最终 VPK | `lmc_c2m1_highway_1b6530c3c5454b7c_final.vpk` |
| VPK SHA256 | `11d721fc850e91eade4d0f832e484c75aa45b2b051bd7a20a8c82923ded00995` |
| BSP SHA256 | `1582197185f9266657e41c949d5dd5af0e315aca2f8427cc07aee6e77ece2954` |

本次基础与 h/l/s 共四份氛围输出各有 135 项字段/参数变化，重复应用不变。原生 HDR 四跳与完整静态模型烘焙通过受保护 lump、面拓扑、静态模型和 detail prop 布置审计。C2/C6→C5 八组历史输出字节等价检查保留原路径行为。

本次引擎日志记录正确地图、guard 后唯一采样请求及成功原生重载，无请求前后拒绝。63 份 HDR cubemap 全部发生变化，导入只替换指定 HDR 资源；非 PAK 数据、原有其他 PAK 内容和采样坐标保持。所有 RGB 半精度值有限；四份原生纹理存在非有限 alpha，按既有规则报告并原样保留，不将其描述为所有通道均有限。LDR 和默认反射仍保留原资源。

最终八文件 VPK 包含原名 BSP、h/l/s、NAV、exclude、addon 描述与原名 soundscape 脚本，官方解包逐字节核验及实际安装 SHA256 核验通过。141 个临时采样文件按收据清理；验收安装中仅有本次 C4 包覆盖 C2，旧 C5 addon 保持停用。本预设未新增降雨、雷电或动态风暴。

人工反馈另存本机 `USER-VALIDATION.md`；保持原 `run.json` 的机器状态与输入清单不变。0.4.0 仅归档本次已接受实现和结论，不更改预设 JSON，不重新烘焙或复用其他运行的捕获。新地图组合和每次新构建仍需独立验收。

## 0.5.0 无注入自动采样验证

2026-09-14 使用正常 ColdClient 启动器，对新的完整烘焙运行执行正式 `auto-capture` 命令。两轮都无需人工控制台操作，实际游戏路径和 `-insecure +exec <本轮CFG>` 参数匹配；等待开场与曝光设置、原生采样、重载审计、正常退出、归档、原名回填、官方打包/解包与临时清理均完成。

| 新运行 | 目标预设 | HDR 数量 | 请求次数 | 最终包文件数 | 清理的临时文件 |
|---|---|---|---|---|---|
| `a11f93c41ff14107`，C2M1 | `c4m3-overcast-static-v1` | 63 | 1 | 8 | 147 |
| `00e4551c769546a0`，C6M1 | `c5m1-daylight-v1` | 48 | 1 | 7 | 116 |

最终 VPK SHA256：C2 为 `e9287cf72065c4af0c12bd718eeb86dbbf812c2d0e1a8496a76c3c43cc6355f4`，C6 为 `edae6afd698f760b7f74d48f4e0d2b14dda51da96058ae49977a6ebe33b55804`。两轮均是全新构建与本轮采样，不复用旧运行的反射。启动器 INI 已恢复原字节；此前验收的 C4/C6 安装包哈希不变，新包只保存在工作区，没有替换已有安装。

前置实验另验证了 C2 重复采样、C6 采样、错误地图守卫拒绝和采样前超时退出；正式实现增加了启动配置校验、进程恢复、同安装互斥及中断归档测试。发行前完整测试为 243 项，其中 242 项通过，1 项因 Windows 缺少创建符号链接权限跳过。独立审查的恢复问题已修复，并验证了缺失旧构建输入时按收据清理及重复恢复。

这里确认的是自动工作流与文件审计通过。机器状态仍为 `final_ready_pending_user_validation`，没有把新包描述为用户已验收。之前对 C4/C6 的人工画面与玩法反馈仍限其原产物。未验证 Steam 正式版的直接 EXE 自动启动、任意插件组合、所有画质/模式、断电恢复或长时间全路线运行。

## 已知风险与后续工作

早期快速烘焙曾出现地面黑线，当前稳定工作流采用完整质量组合；这不保证所有安装、地图和机位完全没有伪影。临时别名缺失反射资源曾造成泛白，因此采样交接提供旧反射路径别名，并在捕获前要求观察材质。游戏启动、实际挂载和曝光仍需实机确认。

后续新构建、其他来源适配或目标参数变化仍分别验证，按证据调整兼容范围。S0—S7 八阶段包、固定机位序列和自动演示尚未实现；当前支持对已生成离线/最终包进行人工截图对比。


## 通用路径阶段 B：C1M1→C5M1（2026-09-16，用户实测无异常）

- 规则 `generic-replace-v1`，运行 `addbcd67c50046a6`；独立源码目录加重新解压的八文件工具包，完整游戏仍提供素材和引擎。未为 C1M1 添加来源名字/坐标分支。
- 基础实体修改 84 项，删除 1 个降水实体、8 条直接视觉输出，新增 1 个初始化实体；保留 1687 条未授权修改的输出。实际模式补丁只有 l_0。
- HDR 烘焙的受保护 lump、面拓扑、叶几何、静态模型和 detail prop 摆放审计通过；compiler_diagnostics 为空。
- 自动正常启动、开场等待、一次采样、原生重载、退出、回填打包和收据清理全部通过；导入 84 份 HDR 反射。启动器 INI 恢复，最终包不包含临时采样脚本。
- 最终 VPK SHA256：`7c565d45ffc53cbb04b98880653cd1f1a1f498ebaed1d8d012026de881e14d25`。状态仍为 `final_ready_pending_user_validation`，不声称酒店电梯、火场路线或其他模式已实测通过。
- C3M1→C4M3 的基础图及 h_0/l_0/s_0 完成参数/保护检查；本阶段未烘焙或实机验收。
- 回归 325 项：322 通过，3 项 Windows 符号链接权限跳过。修复的归档测试夹具缺失不改变转换运行代码。

### 本轮人工反馈归档

用户反馈已保存截图、整个测试过程未发现问题；由于 C1M1 本身为白天，主观风格差异不明显。按先前约定，以参数检查加人工无异常/不劣化记录本轮已观察范围内验收通过。模式为 coop，电梯、火场、安全屋未逐项单独反馈，不扩展为全部事件或其他模式已验证。

手动日志确认原地图 c1m1_hotel、天空 sky_l4d_c5_1_hdr、HDR 开启且级别 2、镜面 1。最终包中的天空、环境光、方向、雾及 cc_c5_main.raw 与原始图不同；保存的单张室内截图不能量化改善幅度。归档包含原始日志、C1M1.png、包哈希和用户原话，位于本地运行的 manual-validation/acceptance.json 及 feedback-20260916。

检查辅助文件存在两处问题并已修正：初始化 logic_auto 的 spawnflags=1 表示触发后删除，不能用其运行时存在性证明转换；内联曝光查询中的分号被控制台拆分，改为独立只读 VScript。该次人工曝光数值未记录，自动采样前的守卫证据保留；不把修正后的辅助查询宣称为已再次实机验证。最终 BSP/VPK 未因文档和检查文件修正而改变。运行清单的 final_ready_pending_user_validation 是工具流程状态，用户验收独立记录，不以修改工具状态代替证据。


## 通用路径：C3M1→C4M3（2026-09-16，用户实测正常）

- 规则 `generic-replace-v1`，预设 `c4m3-overcast-static-v1`，运行 `4041e2b3a56f4baf`。沿用独立源码目录和解压后的八文件原生工具包，转换代码与此前 C1M1 验证时逐文件一致；本轮未增加 C3 专用规则。
- 基础图及 h/l/s 三份实体补丁均处理。每份修改 66 个实体，删除 2 个降水实体及 7 条直接视觉输出，增加 1 个初始化实体；基础图/l/s 保留 316 条未授权修改输出，h 保留 310 条。
- VRAD 烘焙审计通过：8300 个 HDR 面，受保护 lump、面拓扑、叶几何及静态模型数据保持，37705 个细节物件摆放不变；compiler_diagnostics 为空。
- 自动启动、开场/材质等待、唯一一次采样、重载、退出、反射回填、VPK 解包验证和收据清理均通过。导入 44 份 HDR cubemap；原资源、非 PAK lump 和采样坐标保护检查通过；启动器 INI 恢复。
- 最终 VPK SHA256：`93a41a017d2f429c35e137364490e4e0c852e524303acfa0654afcd527a81133`，8 个文件（含三份模式补丁及 soundscape 资源）。已安装原名包和验收辅助文件，用户反馈 coop 实测正常；不声称其他模式已实机验收。
- 原始天空 sky_l4d_night02_hdr → sky_l4d_c4m4_hdr；调色 cc_c3_main.raw → cc_c4_return.raw；雾结束距离 3400 → 1500；曝光上限预设为 10。C4M3 预设的天空资源名采用 c4m4 属于既有预设定义，无新增雨雷。
- 本轮未改转换程序，复用已验证的 325 项回归基线并核对运行代码/原生包哈希；本轮新增验证是实际原生构建和自动游戏采样，没有宣称重新执行过整个测试套件。

### C3M1 人工反馈归档

用户反馈“C3M1正常”，并保存 C3M1.png。截图可见灰绿色阴天和远景雾，当前画面未见明显黑线、缺失材质或武器反光泛白；单张截图不覆盖全图。手动日志确认原名 c3m1_plankcountry 加载，已部署最终 VPK 与记录哈希一致。日志保留脚本性能、UI 字体等提示，不把用户无异常反馈等同于日志零警告；本轮没有执行或记录可选的手动参数查询。

结合既有参数、光照及 44 份 HDR 反射审计，记录本轮在用户实际观察范围内验收通过。具体路线节点未逐项报告，其他模式未验收。截图、原始日志、用户原话及哈希已保存在本地运行的 manual-validation/acceptance.json 和 feedback-20260916；工具流程状态不代替独立的人工作业记录。


## 通用天气 v2：C6M1→C5M1（2026-09-16 构建，09-17 带已知画质限制接受）

- 显式规则 `generic-replace-v2`，预设 `c5m1-daylight-v1`；运行 `1601bca91a184edc`，本机目录 `runs/c6m1-c5m1-generic-v2-01`。源码 ZIP 解压到独立目录，重新解压八文件原生工具包执行，无地图名/坐标/Hammer ID 天气分支。
- 基础图及 h/l/s 处理通过；基础图保留 1029 条受保护 IO，删除降水及证据明确的声音/混音端点共 6 个。模板风归零并保留生成/销毁生命周期。新娘 Witch 的全部字段和输出、两处独立玩法震动的实体字段及输出与源一致；这些是静态证据，不代替事件实测。
- HDR 烘焙审计通过：10693 个面、6113 个 detail prop 布置保持；受保护 lump、面拓扑、叶几何、静态模型布局和实体数据保持，compiler_diagnostics 为空。
- 正常启动器自动启动、开场/材质等待、一次采样、重载、退出、48 份 HDR 回填、原名 VPK 解包及临时资源清理通过，启动器 INI 恢复。全部 48 份 HDR 与本轮烘焙基线不同，非 PAK lump、原有其他 PAK 内容和采样坐标保护通过。仅 HDR 资源更新；原生非有限 alpha 按既有规则报告并保留。
- 最终七文件 VPK SHA256：`e30886a4741666d1ec52f144361eb97e4f2fce4534b9851ad575db9c45ff7701`；BSP SHA256：`c165ae6de128ad2ee74f0d426bb0969b3ab7cabd3568211b8954d8b0186f3a37`。状态 `final_ready_pending_user_validation`。
- 回归 337 项：334 通过，3 项 Windows 符号链接权限跳过。新增保护涵盖共享事件、相同实体的不同事件、模板风、震动物理标记运行时改写、共享震动状态、混合音效调用、严格命令匹配及运行清单规则降级拒绝。独立审查后修复的三项问题均有回归用例。
- 保留并报告 10 条天气事件中没有静态目标的输出；未按名字猜测删除。外部脚本和未知粒子继续保留。人工需检查普通路线、原天气触发后的晴天保持、新娘 Witch 尸潮以及反射/光照；不继承旧 C6 专用适配验收，也不声明其他模式已实测。

### 最终原名加载后的待排查项

本轮最终包部署和首次原名加载成功，但约三分钟后日志记录 `Server shutting down` 并返回主菜单，游戏进程仍在。用户确认不是主动退出。在该首次加载节点，尚不能记为正常或已验收（后续重试与人工结论见下文）。已保留初次日志和部署哈希，并准备临时诊断 CFG/VScript 记录空闲配置、玩家队伍状态及服务端日志；当前尚未确定原因，未改转换产物或把此问题归咎于地图。

用户随后说明当时也在操作电脑，并在手动退出后让出操作。正常启动器重试同一最终包，连续超过四分钟仍在原名地图，未再次出现服务端关闭。未修改地图或空闲/玩法参数，首次返回的原因仍未确定，不宣称修复了地图崩溃。临时诊断中的 GetTeam 查询不受原生接口支持，队伍心跳未作为证据；持续地图时间、原名加载及服务端事件可用。诊断停止 CFG 可移除临时计时实体并关闭额外日志，最终画面和事件验收仍待用户。

补充实际 v1 回归：与此前独立目录比较 C1M1/C3M1 的 BSP 和实际模式补丁，共六份容器的转换输出字节一致；审计除预设所在绝对路径外一致。

### C6M1 人工反馈与局部过亮记录（2026-09-17）

用户反馈“没有什么特别的异常”，并指出婚礼区域白色塑料椅偏亮；随后提供同机位 Phong 开关两张截图，表示未见明显区别，允许先记录并继续。结论为本轮在实际观察范围内带已知画质限制接受，不宣称全图画质无缺陷。未逐项反馈 Witch、路线节点及其他模式，不扩大验收覆盖。运行机器状态保持不变，人工结论另存本地 `manual-validation/acceptance.json`，截图与哈希归档于 `chair-brightness-20260917`。

**VIS-001：局部白色动态/物理模型过亮。** 三个椅子矩形区域在 Phong off/on 图中的 RGB 平均绝对差分别约 0.052、0.064、0.054（满值 255），武器区域约 77.27；椅子仍明显偏亮。因此不再把原材质 Phong 高光作为已证实主因。矩形含背景，数值只用于比较截图，不是辐射亮度测量。基础图和 h/l/s 的 62 个塑料椅实体参数均与原图一致；材质未配置 envmap，未发现当前顶层 addon 覆盖对应模型或材质。模型受光、局部灯光与曝光的贡献尚未分离，根因未确定。

用户允许暂缓此项。保留现有最终包，不以全图压暗、关闭全局高光或给单个地图的椅子改色作为修复。今后若跨地图反复出现，可按通用模型受光问题重新调查。首次回菜单及游戏控制问题按用户要求暂停，未声称已修复；再次发生时再收集证据。

本轮仅新增证据和文档，最终包 SHA256 保持 `e30886a4741666d1ec52f144361eb97e4f2fce4534b9851ad575db9c45ff7701`。没有重复烘焙、采样或扩大历史 337 项回归的执行声明。

## 批量只读预检查（2026-09-17）

新增 `batch-check`，沿用单图配置和检查算法；本轮不改变转换规则、不运行原生工具或游戏，不增加实机验收地图。源码工具包解压到独立目录执行 8 项官方地图组合，输入使用本机完整游戏，工具路径指向此前解压的八文件工具包（本轮仅校验其存在与哈希）。

| 输入 | 预设 | 规则 | 预检查 |
|---|---|---|---|
| C1M1 | C5M1 | v1 | 通过 |
| C3M1 | C4M3 | v1 | 通过 |
| C6M1 | C5M1 | v2 | 通过 |
| C2M1 | C5M1 | v2 | 通过 |
| C4M3 | C5M1 | v2 | 通过 |
| C7M1 | C4M3 | v2 | 通过 |
| C8M1 | C5M1 | v2 | 拒绝：混合视觉/非视觉 IO 目标 |
| C13M1 | C4M3 | v2 | 通过 |

7 项通过各含 8 条局限/覆盖警告，不代表全资源闭包或运行时效果通过。C8M1 在 conversion_plan 阶段报告 `Mixed visual/nonvisual output target: 298/6`，归为 conversion_rejected；此为当前拒绝点，根因及可泛化处理方式尚未调查。未放宽保护检查，后续 C13M1 仍完成。失败项保留配置、声明输入及检查前标准化清单哈希。

独立目录复测的 7 项完整 preflight 与工作区首次执行一致（仅预设安装绝对路径不同），所有未来构建目录仍不存在。证据在本地 `artifacts/batch-preflight-20260917/report-02`，复测源码包 SHA256 为 `6c6597ed54a0cb96dabe41a86460fb50cb3b3476a14b8617b701610b9c95efa3`。该包早于本段结果归档，程序哈希另见报告。

完整回归 351 项：348 通过，3 项 Windows 符号链接权限跳过。新增 14 项批量测试覆盖单图结果等价、不构建、独立失败、报告隔离、清单路径保护、中断检查点、内部异常分类及配置/旧参考图变动。审查发现的参考图哈希遗漏和失败取证丢失均补回归并修复；该轮尚未进行批量构建，后续见下文。

## 首次串行批量构建与采样（2026-09-17 至 09-18）

首版 `batch-run` 从独立源码目录执行两项清单，源码 ZIP SHA256 为 `c873e2831799cdad3c4a5ff15f47581954a11be17f400afcb9cb9146f8156f86`。重新解压既有八文件原生工具包并校验清单，未使用官方安装 bin 作为执行工具路径。执行模块与工作分支逐文件一致；后补结果文档不在该次实验源码包内。

| 输入 → 目标预设 | 运行 ID | HDR 光照面 | HDR 反射采样 | 结果 |
|---|---|---:|---:|---|
| C7M1 → C4M3 | `b80e9e025ee44731` | 6801 | 37 | 机器完成，待人工验收 |
| C4M3 → C5M1 | `5d54011b33ad4347` | 11578 | 37 | 机器完成，待人工验收 |

两项均显式使用 `generic-replace-v2`；预设分别为 `c4m3-overcast-static-v1` 与 `c5m1-daylight-v1`。批次为 complete，ready=2 / failed=0 / pending=0。实际顺序为第一图构建→采样→退出→最终打包/清理→第二图同流程；没有人工启动或控制台参与采样，未触发重试、恢复或失败分支。

- 两图均处理基础 BSP 和 h/l/s 补丁，基础图分别保留 369 / 261 条受保护 IO。C7M1 没有识别出的天气事件；C4M3 有 14 个天气事件、5 个天气端点、3 条未解析天气输出，继续报告，不按名称猜测删除。静态检查不证明脚本天气全部消失或玩法全程通过。
- 烘焙审计均通过，compiler_diagnostics 为空；面拓扑、叶几何、静态模型布局及细节物件摆放保持，细节物件数分别为 5039 / 14079。C7M1 按已验证的当前模型布局处理了一项 forklift VHV checksum 迁移；C4M3 没有模型布局迁移。
- C4M3 重算了 7 个叶节点的 SKY/SKY2D 标志，属于既有资源挂载模式允许的字段；其 protected_lumps_unchanged 为 false，不能说全部保护 lump 字节一致。逐字段审计确认几何、区域及其他受保护字段不变。C7M1 没有叶天空标志变化。
- 每图一次 CAPTURE_SENT，均完成开场/材质等待、重载、原生反射审计和 GAME_EXITED。全部 37 份 HDR 与本轮各自烘焙基线不同；非 PAK lump、原 PAK 内容、采样坐标保护通过，LDR/default 资源保留。两个最终包均通过原生解包校验和哈希复核。
- 两轮结束后原始启动器 INI 哈希恢复；清理收据的文件全部不存在，游戏进程已退出。游戏日志仍有 HUD 材质/字符串表等提示，未将机器完成表述为零运行日志警告或画面已验收。

最终包 SHA256：C7M1 `55e879532c34cecf7728c9fc7e6cb6547b6a6542214c2489a0440e4f404fb402`；C4M3 `9add24184b0b6f392ea8e8d86915ec7d6c0f3ba4a054b2331f25e242f8f6bffa`。本地证据为 `artifacts/batch-run-20260917/report-01`、`machine-verification.json` 和各自 `runs/batch-01-*`；机器状态仍为 final_ready_pending_user_validation。

批量命令没有部署最终包。实验结束后另行核对 addons 及子目录的 15 份 VPK 目录树/松散 BSP，没有发现这两张图的覆盖冲突，随后为用户验收新增两份原名最终 VPK 和四个加载/查询 CFG。没有覆盖旧包或修改玩法；部署证据位于 `validation-deployment.json`，human_acceptance 仍为 not_run。本轮尚无人工视觉/路线验收结论。

完整回归 366 项：363 通过，3 项 Windows 符号链接权限跳过。新增 15 项协调器测试覆盖任务顺序、预期失败继续、采样/环境/内部异常停队、清单和输入变动、已有目录保护、中断和最终包/清理证据。审查发现预检查 internal_error 未阻止后续构建，已用红/绿回归修复并独立复核。测试替换原生构建/游戏边界，以上两图实际独立包运行作为集成证据；没有因此声明所有地图或失败恢复均已实机验证。

### 两图人工反馈与暂缓观察项（2026-09-18）

用户完成测试，反馈 C7M1 没有问题；C4M3 总体没有问题，但晴天仍有无声闪电，部分管道/储罐表面反光较明显，呈“雨天刚晴”的感觉。用户认为这两点暂不算 BUG，要求技术分析和记录，先不修。记录 C7M1 为实际观察范围内接受，C4M3 为带已知视觉限制接受；未逐项报告路线/事件，不扩大为全图、全战役或其他模式均通过。

截图 C7M1-batch-C4M3.png、C4M3-batch-C5M1.png、C4M3-batch-C5M1-反光.png 及当时的手动日志快照，已分别归档到各运行 `manual-validation/feedback-20260918`，附 SHA256；每个运行的 acceptance.json 绑定原最终包哈希。批次原有机器状态/待办快照不改写，人工结果另存 `artifacts/batch-run-20260917/manual-acceptance-20260918.json`。三张静态截图不能重现闪电的时序；该现象以用户实测反馈为准。

**VIS-002：晴天残留无声闪电，天气粒子覆盖缺口，按用户要求暂缓。** 基础 BSP 和 h/l/s 四份最终实体容器均保留 15 个天气粒子实体：1 个 storm_cloud_parent（start_active=1）、13 个 storm_lightning_02、1 个 storm_lightning_screenglow，字段与自身输出均与原图一致。relay_lightning_strike、case_lightning_strike、timer_storm_lightning_strike 及其粒子 Start/Stop 链仍在，storm relay 仍能调用这条视觉链；原 Weather.thunder_close_all_4 音效端点已移除。这足以解释“视觉闪电仍可发生而雷声已取消”的静态机制，但本轮未运行时隔离具体哪一个粒子产生了用户看到的闪电。

当前规则明确保留未纳入目录的粒子，避免误删烟火、火焰、场景或玩法提示。因此这是已识别的覆盖边界，不是重新烘焙生成了闪电，也不能仅归因于此前报告的 3 条未解析输出。后续技术上可以增加天气粒子资源目录和晴天策略，同时处理初始激活及后续 Start/Stop/模板重建，审计控制点依赖；只修改有依据的纯天气效果端点，保留共用 storm relay 上的 Director、台词等其他输出。无需重建地图几何，但仍需生成新产物并实机确认不会重新激活；当前不实施。

**VIS-003：晴天环境下保留较强表面光泽/湿润观感，按用户要求暂缓。** 截图可见近处弧形金属表面的宽亮带；本轮未准确绑定该像素对应的模型、skin 和最终加载 VMT，也没有同机位原版对照或反射/高光分离实验，因此根因仍是有证据支持的推测，不能断言都是雨水材质或 cubemap 错误。

已证实：原图纹理名称 lump 保持；BSP 内原有 141 个 VMT 字节不变，仍存在 blend_blacktop_*wet、highway_02wet 等湿地面引用。安装资源中候选 PipeSet32d 与 Tank_large 材质使用 `$envmap env_cubemap`、反射掩码及 `$envmaptint [.8 .8 .8]`；另一些冷却罐/管材候选使用 `$phong` 和 Fresnel 设置。上述候选并不证明截图特定表面实际绑定它们。反射内容已按新环境重采样 37 份 HDR，但转换并未将原有材质改为干燥、低光泽版本。

技术推断是：新光源方向、天空/环境反射及曝光，与原材质保留的反射和高光响应叠加，可能强化“雨后初晴”外观；停雨不会自动让一切表面失去光泽。Phong 高光还会受材质掩码和指数影响，参见 [Valve 的 Source 着色技术说明](https://cdn.cloudflare.steamstatic.com/apps/valve/2006/SIGGRAPH06_Course_ShadingInValvesSourceEngine.pdf)。本轮没有 A/B 实测，不能量化各项贡献，也不应把此前 C6 白椅问题直接套用到这里。

后续可提供可选的材质外观策略：保留原材质，或对明确识别的湿材质/过强反射做干燥版本映射及有限参数调整。适当保留金属应有的反光，不以全局关闭镜面或压暗整张图处理。模型共享材质若需修改，应规划限定本次地图使用的独立资源，避免全局同名 VMT 覆盖影响其他地图；材质/贴图本身表达的湿痕，也未必只调一个反射参数就能消除。这属于对当前“局部灯光与材质默认保留”的可选扩展，当前不实施。

本轮只归档和分析，未修改程序、预设、地图、材质、IO 或游戏设置，没有重复烘焙/采样或宣称执行新的回归套件。两个最终包哈希与上节相同，C6 的 VIS-001 仍单独保留。
## 2026-09-18：generic-replace-v3 去闪电，C4M3 复测准备

用户将 VIS-002 从暂缓观察项改为本轮实现项：原图转为无风暴目标时移除已知闪电，策略不得绑定 C4M3。VIS-003 的管道/湿润材质光泽仍暂缓，VIS-001 白椅及旧游戏控制问题不在本轮范围内。此前 v2 接受结果与缺陷观察保持为历史记录；本轮人工结论尚待反馈。

- 新规则：显式 `generic-replace-v3`，继承 v2 并使用 `weather-assets-v2` 的准确粒子资源标识；移除 `storm_cloud_parent`、`storm_lightning_02`、`storm_lightning_screenglow` 端点及已知直接控制输出。识别不依赖地图名、实体名、Hammer ID 或坐标。保留共享上游控制器、玩法输出、未知粒子；脚本、混合目标、模板/父子/保留控制点依赖拒绝并报告。两种现有目标均为无风暴预设，不推断未来雨天目标。
- 自动回归：375 项，372 通过，3 项因 Windows 符号链接权限跳过；新增粒子、共享玩法、模式补丁、旧规则及采样身份保护。独立代码复核发现并修正两个待删除粒子互为控制点时的过度拒绝，补有回归测试。
- 八图实体转换对照：C1M1、C2M1、C3M1、C6M1、C7M1、C13M1 的实际 BSP/LMP 在 v2/v3 下输出字节相同；C4M3 基础图及每份 h/l/s 补丁各新增删除 15 个粒子、32 条直接粒子输出，其他字段/新增实体规则相同；C8M1 基础图及实际 l/s 仍以原有混合目标错误拒绝，没有放宽保护。此项是离线对照，不能扩大游戏验收范围。
- 独立目录运行 `c4e6c520aae9489f`：从源码 ZIP `b7ea9a230276cd1a03b9b5911233a9894681ca287767b4a6bc0e95bc675d4a88` 和既有八文件原生工具 ZIP 新解压，重新构建 C4M3→C5M1，再自动启动正常启动器、单次采样、重载检查、正常退出、回填和清理。转换 Python 代码与工作区逐文件一致；后续文档记录不改变运行代码。
- 原生检查：11,578 个 HDR 面、14,079 个 detail props，编译诊断为空；叶几何、面拓扑、静态模型布局、NAV 和 exclude 文件保持。与上一轮一致，resource_roots 挂载下允许并记录 7 个叶节点 SKY/SKY2D 标记重算，因此不能声称所有受保护 lump 字节完全不变。
- 反射回填 37 份 HDR cubemap；最终 VPK 原生解包验证通过。解包后的基础图及 h/l/s 均不含上述三类粒子；141 份原始内嵌 VMT 与纹理名称数据保持，不调整材质响应。最终包 SHA256：`e565c7b12348b4fbcf6b3924c8c4d74048b7388413c9efa4ab5059ae106851f2`。
- 本机证据位于 `artifacts/lightning-v3-20260918/`（新旧规则差异、machine-verification、独立源码/工具、部署收据、复测指南）；运行目录 `runs/lightning-v3-c4m3-c5m1-01/`。这些本地大文件和游戏资源不加入源码仓库。
- 待人工验收：正常启动测试版游戏，执行 `exec lmc_lightning_v3_validate`，进图后执行 `exec lmc_lightning_v3_info`。室外可执行 `exec lmc_lightning_v3_probe` 主动调用原粒子闪电 relay，再正常游玩观察数分钟。这个源图名称仅用于本地回归探针，不进入产品通用规则。旧 C4 包可逆停用保留，C7 包与其他插件保留。

### v3 人工接受与原图隔离检查（2026-09-18）

用户随后确认没有闪电或闪光、符合预期，接受 VIS-002 通用规则的本次 C4M3 实测结果。人工反馈绑定 `c4e6c520aae9489f` 最终包，保存于该运行的 `manual-validation/feedback-20260918-v3`；不扩大为所有地图、全流程或其他模式验收。

按用户要求检查 VPK 隔离：最终包仅包含 addoninfo、BSP、NAV、exclude 和 h/l/s 补丁七个条目，没有全局材质或配置覆盖；原 BSP 的 141 份内嵌 VMT 未变。27 份历史原始文件记录、19 项本次输入及 447 项模型资源快照核对通过，自动采样临时文件均已清理，启动器配置哈希恢复。证据为本地 `artifacts/lightning-v3-20260918/package-isolation-audit.json`。

禁用全部同图覆盖包后完全重启游戏，地图内容将重新从原安装加载；本轮检查没有代替实际运行原图的 A/B 实测。单独留下的手动 CFG/日志不属于 VPK；手动执行的画质或调试 cvar 是游戏设置，不能宣称 VPK 停用会恢复任意此前设置。

### 可选材质反光策略：C4M3→C5M1（2026-09-18）

用户随后授权实施 VIS-003 的可选材质优化。新增显式 `catalogued-static-reflections-v1`，默认 preserve 仍保持原行为；规则按 MDL 实际材质引用匹配四项资源目录，不绑定地图名。只将受支持材质的显式 envmaptint/Phong boost 乘 0.5，不改变 Fresnel、贴图湿痕或全局渲染开关。

- 自动回归 386 项：383 通过、3 项因 Windows 符号链接权限跳过。覆盖默认字节等价、私有路径、原 PAK/实例保护、复杂材质跳过、缺碰撞资源无部分写入、输入变化及策略身份漂移等。独立代码复核未发现待修项。
- 从独立源码包 `2406dc4d3dceec00b6245129243aae69edb9364f3ee0f9f666b5fca4977ccb36` 及重新解压的八文件工具包运行，32 个执行 Python 模块与工作区一致；该实验包早于后补结果文档和示例。
- 运行 `0b6ed222f24e47b2` 识别 8 个模型族、40 项私有资源：六种管道、锅炉和冷却罐。管道 envmaptint 从 0.8 到 0.4、锅炉从 0.5 到 0.25、冷却罐 Phong boost 从 0.1 到 0.05。`tank_large01` 的实际材质不匹配目录，未按模型名字误改；四项无关门框模型的不支持材质路径在 skipped 中报告。
- 原生烘焙检查通过，compiler_diagnostics 为空；11,578 个 HDR 面、14,079 个 detail props，面拓扑、叶几何及静态物件实例保持。7 处 SKY/SKY2D 标志仍按既有允许规则重算，不能声称全部 lump 字节不变。模型引用变化只在材质准备阶段发生，之后烘焙保持完整 sprp 内容。
- 自动正常启动、单次采样、重载、退出、回填 37 份 HDR cubemap；最终包经原生解包校验，SHA256 为 `2381af3c706cc83d03c9b8797f4e12324ef7715701e7d78345d63b33d40f478b`。机器状态 final_ready_pending_user_validation；画面是否更自然仍待用户反馈。
- 最终隔离检查通过：原 141 份 VMT 字节保持、40 份私有资源哈希保持，复制的 VVD/VTX/PHY 与原资源一致，sprp 实例尾部不变；实体、h/l/s、NAV、exclude 与接受的 v3 版本一致。未发现私有资源加载错误，94 项清理收据路径已不存在，启动器 INI 原哈希恢复。未据此宣称游戏日志完全无警告。

本机证据位于 `artifacts/material-policy-20260918/` 和 `runs/material-c4m3-c5m1-01/`。本轮不将目录支持范围扩大为任意材质自动干燥，不宣布 VIS-003 已经由用户接受，也不修改原 C6 白椅 VIS-001 的记录。

#### 材质版人工反馈：残留反光暂缓

2026-09-18 用户复看后反馈“还是有反光”，保存 `C4M3-material01-反光.png`，要求优先保持隔离、暂不继续修复，转入扩大样本。截图中管道仍有亮带；不能据此把有限参数减弱称为湿润外观已消除，也没有精确匹配机位和曝光的 A/B 测量来量化差值。

截图、SHA256、运行及最终包身份已保存到 `runs/material-c4m3-c5m1-01/manual-validation/feedback-20260918-residual`。VIS-003 记录为用户要求暂缓，材质策略保持可选实验项、默认 preserve；此反馈不扩大为其余材质或完整玩法的验收。私有资源隔离的机器结论保留，与外观是否达到用户目标分开。后续扩大样本使用默认材质保留策略，避免将尚未确认的视觉调整作为通用必选行为。

## 扩大输入样本：C13M1 模型版本阻断（2026-09-18）

使用源码提交 `1bd4989` 的独立发布包及重新解压的八文件工具包，选 C13M1→C4M3、generic-replace-v3、默认材质保留。实体/IO/预设预检查通过，实际 build 在 preparing 阶段的 ModelLightingEvidence 拒绝 `models/props_foliage/tree_pine_large.mdl`，尚未启动 VRAD 或游戏，也没有最终转换包。

最小只读复现重复产生相同错误。按配置资源顺序实际解析到 `left4dead2_dlc3/pak01_dir.vpk`，资源头 IDST、版本 44、长度 1,708 字节，SHA256 `db96e21a4b8e8a4899d4d436ac4afe149cb0231afd12c92c70ff6b344f6c3785`；现有审计明确只接受版本 49。这里只证明当前安装与审计契约不兼容，不把版本号改写成 49、不推断游戏引擎一定无法加载，也不声称其他安装的 C13M1 具有相同资源。

记录为 MODEL-001：当前资源的旧版模型支持缺口，后续须核对对应格式、原生工具加载行为和光照/拓扑保护再扩展。本轮不放宽保护、未修复。证据位于 `artifacts/sample-expansion-20260918/c13-model-blocker.json`、`c13-repro.log` 及 `runs/sample-c13m1-c4m3-v3-01`。预检查通过仍不能被列为成功转换或实机验收；继续选择 C5M1→C4M3 作为下一份完整样本。

### C5M1→C4M3：离线阶段完成，待引擎采样

同一独立目录改用 C5M1 输入、C4M3 静态阴天预设、v3 及默认材质保留。运行 `5a3aef3dc95f49d4` 已达到 offline_ready：6,362 个 HDR 面、2,096 个 detail props，面拓扑、叶几何、完整 sprp 和受保护 lump 保持，SKY/SKY2D 标志变化为 0，compiler_diagnostics 为空。发现并处理当前安装中的 h/l 模式补丁，没有杜撰不存在的 s 补丁。

离线包经原生解包复核，七个条目，SHA256 `47b57563f90983562ec49aec7c5728cf9d2d07e095c32950d73b230573a7a12e`。只读扫描 17 个 addons VPK 未发现 C5M1 覆盖冲突；尚未部署最终包。当前用户上一轮游戏进程仍运行，已请求退出，故停在自动采样前；不把 offline_ready 称为完整端到端或人工通过。程序未改动，本轮无需重复代码回归，使用该地图真实构建审计验证现有流程。

材质版反馈后另核对 19 项原始输入与已部署文件哈希均不变，证据 `material-isolation-recheck.json`；仍未执行禁用 VPK 后进入原图的运行时 A/B 实验。下一步在游戏关闭后继续 C5M1 自动采样、最终验证与人工部署。

### C5M1→C4M3：自动采样与最终校验完成

用户确认退出后，继续同一运行 `5a3aef3dc95f49d4`，未重复烘焙。自动启动正常测试版启动器，单次 CAPTURE_SENT、重载、正常 GAME_EXITED，回填 41 份 HDR cubemap，全部与本轮烘焙基线不同；严格资源审计确认非 PAK lump、原 PAK 内容、采样坐标记录保持，LDR/default 反射保留。仅控制台状态标记不作为采样完成证明，以最终 BSP 审计为准。

最终包 SHA256 `feab6116871bdef025c2f430f330bc46db0b1d8a1ff20285f4d538be99d67ea1`，原生解包及逐文件哈希通过，七个条目包含 BSP、NAV、exclude、h/l 补丁、addoninfo 和限定 C5M1 的 soundscape 文件。22 份原始内嵌 VMT 保持，NAV/exclude 与输入字节一致，使用默认材质保留。102 项采样清理收据路径均已不存在，启动器 INI 原哈希恢复；游戏日志仍有 HUD 材质、别名地图可选文件缺失、现有 SourceMod 更新联网失败等提示，不宣称日志零警告。

机器证据 `artifacts/sample-expansion-20260918/c5-machine-verification.json`，状态 final_ready_pending_user_validation。本项机器端到端已完成，但尚未收到 C5M1 的人工画面/路线反馈；因此不能将本输入加入已人工验收清单。C13M1 的 MODEL-001 和材质 VIS-003 仍保留，未修改算法以隐藏这些边界。

最终部署前再次检查游戏关闭、同图覆盖冲突及目标路径不存在，新增一份 C5M1 VPK 和 `lmc_sample_c5_validate` / `lmc_sample_c5_info` 两个手动 CFG；安装后哈希复核通过，原 C4/C7 包及其他插件保持。收据为 `c5-validation-deployment.json`，具体人工步骤为同目录 `manual-c5-validation.zh-CN.md`。此部署是本次测试辅助操作，不代表 batch-run 已新增自动最终部署功能。

### C5M1→C4M3 人工接受

2026-09-18 用户确认“没有问题，本轮可以验收成功”。将上述最终包记录为实际观察范围内接受，反馈与包 SHA256 绑定在 `runs/sample-c5m1-c4m3-v3-01/manual-validation/feedback-20260918-accepted/acceptance.json`。本轮反馈未附新截图，不虚构截图证据，也不扩大为全战役、全部模式或其他输入通过。

## C7M1 目标预设：提取与离线准备（2026-09-18）

用户随后指定下一个目标 C7M1，先提取预设和说明通用方案，再选择输入地图实测。新增 `c7m1-hazy-static-v1`，schema 3，内容 SHA256 `69580cc6cf0237dbd63ddbabbe7bfe9a72db79625c2172c2cf2f644f8568da7f`。完整参数、排除项及使用方法见 [C7M1 说明](c7m1-preset.zh-CN.md)。

- 原始 C7M1 基础图及 h/l/s 的 24 个氛围角色参数一致（排除身份/位置字段），17 条本次筛选出的相关输出也一致；其中固定曝光/Bloom 来自 logic_auto 初始化。选常态室外，不采用坦克/窑炉/开局局部效果。脚本入口仅记录，不声称解释了任意运行时脚本。
- 通用能力扩展：schema 3 支持空的额外方向光、按玩家角色区分的曝光/Bloom 和受限太阳材质路径。转换分支不依赖 C7 地图名、实体名或坐标，方向光删除沿用玩法/生命周期保护；guard 使用幸存者 max 9。旧预设 JSON 保持，旧 source_profile 入口不接受 C7。
- 11 项直接资源与已观察的 7 个 PC 天空/太阳纹理依赖均可读取并记录来源/哈希。环境音只选非定位底噪；不导入 Valve 关卡脚本、音频或纹理到源码。该资源检查不是任意输入地图完整依赖闭包证明。
- 8 项真实地图预检查：C1M1、C2M1、C3M1、C4M3、C6M1、C7M1、C13M1 通过，C8M1 仍因 `Mixed visual/nonvisual output target: 298/6` 拒绝。7 项通过均保留 8 条覆盖警告；C13M1 既有 MODEL-001 烘焙限制仍然存在。预检查没有生成这些地图的运行目录、BSP 或 VPK。
- 旧 C5M1/C4M3 在这 8 张输入的实际基础及现存 h/l/s 容器上，对照改动前独立源码：54 组结果一致，其中 48 组输出逐字节相同、6 组拒绝一致。独立代码复核另确认合成 BSP/LMP 在通用规则 v1/v2/v3 下的旧输出及报告一致，并检查方向光删除的输出、全局生命周期、模板和父子保护，没有待修发现。
- 完整回归 392 项：389 通过，3 项 Windows 符号链接权限跳过。新增角色曝光、缺失方向光保护、schema 非法值、资源/发行包和采样 guard 测试；发布包在独立目录解压后再跑相关 26 项，25 通过、1 项权限跳过。独立包 SHA256 `f2f5b45b6c948e2d7e27e4918feeb6fe37917ff58bd7e8376b046cc9e9a3b3d5`，后补本段文档不改变测试代码。

本机证据在 `artifacts/c7m1-preset-20260918/`。本轮没有 VRAD 构建、游戏采样或部署新 C7 风格地图，没有 C7 目标的人工效果结论；下一轮建议使用原始 C2M1，便于对照已有 C5M1/C4M3 结果。合作模式验收不能代替感染者／ghost 视角验证。

### C2M1→C7M1：机器流程完成，待人工验收

2026-09-18 用户指定先用 C2M1 应用新预设。运行 `3db9105c6c30459b`（`runs/c2m1-c7m1-v3-01`）使用原始 C2M1、generic-replace-v3 和默认 preserve 材质策略。输入 SHA256 `d07bca1e51805ae0feedea28edc8f31d9a48645ef0b74ee35547ca58b3e3aaf9`，与本机先前提取的官方 C2M1 字节一致，并非在旧风格包上叠加转换。

从提交 `b7503c8` 对应的独立发布包解压执行，代码包 SHA256 `1e56e2d6adde4206308704516de5dce345af7436cc2084fb6a3d3e344afae09c`；八文件 Windows 原生工具包重新解压，SHA256 `c966088e05a9d8ea59f9e46b5f247b874c37e86c63f0f82d14518161a05550a8`。本轮没有修改转换算法或加入 C2 专用分支。

- 基础 BSP 及 h/l/s 补丁的受保护玩法 IO 检查通过；最终四个容器的氛围角色、角色曝光/Bloom 初始化与预设逐项一致，无额外 light_directional。幸存者曝光 max 9/Bloom 0，感染者及 ghost max 3/Bloom 1；后两者仅做参数检查，未验收对应视角。
- VRAD 完成 5,764 个 HDR 面和 38,443 个 detail props；面拓扑、叶几何、sprp、detail 布置及受保护 lump 检查通过，SKY/SKY2D 标志变化为 0。compiler_diagnostics 为空。
- 自动启动正常测试启动器，单次 CAPTURE_SENT，经重载、结束采样与正常 GAME_EXITED 后生成最终包；63 份 HDR cubemap 全部更新，严格资源审计通过。保留原 LDR/default 资源；原生输出中四份纹理含非有限 alpha，按现有策略报告并保留，所有存储 RGB 有限。状态标记本身不作为画面正确性的证明。
- 147 项临时采样路径均已清理，启动器 INI 恢复原哈希。原有 207 份内嵌 VMT 字节保持，NAV/exclude 与输入一致。游戏日志仍有可选别名文件缺失、现有 SourceMod 联网更新和 HUD 材质提示，不宣称日志零警告。

最终 VPK SHA256 `3de958bc9ac18d1ea118dbd7fec6807d60dd25aa35926879858fb7bec7cf678b`，原生解包及八个条目逐文件哈希通过，包含 BSP、NAV、exclude、h/l/s 补丁、addoninfo 和本图 soundscape。状态 `final_ready_pending_user_validation`；机器检查通过不等于人工画面接受。

部署前确认游戏关闭、没有其他 C2 覆盖冲突，将旧 C2→C4 VPK 可逆改名为 `.vpk.disabled-before-c7m1`，新增本轮 VPK 及 `lmc_c2_c7_validate` / `lmc_c2_c7_info` CFG，安装文件与备份哈希复核通过；原图和其他插件保持。此步骤是本次测试辅助部署，不代表 batch-run 新增自动最终部署功能。

本机证据在 `artifacts/c2-c7m1-20260918/`：`provenance.json`、`input-origin-check.json`、`machine-verification.json`、`validation-deployment.json` 和 `manual-validation.zh-CN.md`。人工检查采用熟悉的赛车/卡车机位及其他室外、室内、遮阴区域，建议截图 `C2M1-C7M1-stage-final.png`。截至本记录尚未收到本轮画面反馈，不将 C2→C7 列为人工验收通过。

### C2M1→C7M1 人工接受与成品保留

2026-09-18 用户反馈“不错，没有什么异常”，提供 `C2M1-C7M1-stage-final.png`。截图为汽车旅馆附近视角，已实际查看并连同哈希及包身份归档到 `runs/c2m1-c7m1-v3-01/manual-validation/feedback-20260918-accepted/`。记录为用户实际观察范围接受，不扩大为全部路线、游戏模式或感染者视角通过。

按用户要求保留已烘焙包供后续场景演示录制。本机 `artifacts/vpk-library/` 另存现有运行的 offline/final VPK，当前包含 31 份、总计 1,090,697,573 字节；每份来源与归档副本 SHA256 匹配，没有缺失或哈希不符。原 runs 输出不删除。目录内 `index.json` / `README.zh-CN.md` 区分输入、预设、阶段、材质策略和对应人工反馈，并保存运行/预设快照及匹配包哈希的反馈附件。该数量包括本轮 C4M3 新包；旧实验或待验收包不会因归档自动算作成功验收。此为本地录制资料库，不将生成的游戏资源提交到源码仓库；录制成品优先 final，offline 可作反射更新前的阶段示意，同图包不能同时启用。

### C4M3→C7M1：第二个输入完成机器流程

同日按用户指定，用原始 `c4m3_sugarmill_b` 应用同一 C7M1 预设，运行 `8c230549caa14c9f`，目录 `runs/c4m3-c7m1-v3-01`。重新解压与上一轮相同的独立代码/原生工具包执行，规则 generic-replace-v3，材质 preserve，未更改转换算法。输入 SHA256 `8d482d83e6639fc4d024a89b4d4b988eeaa1ae44d1947221a07dd9d7a7172462` 与先前 C4M3 测试的原始输入一致，并非旧材质实验包。

- 基础图及 h/l/s 受保护实体与 IO 检查通过，受保护 IO 分别 229/223/229/229 条；最终四个容器的角色参数、曝光/Bloom 初始化和无额外方向光均与预设一致。
- 通用天气目录识别 15 个闪电粒子、1 个雷声端点及 4 个由独占天气写入者控制的资源端点并处理。仍报告 3 条未解析天气输出引用（sound_thunder2、timer_storm_blendin、timer_storm_blendout）；不凭名字猜测实体所有权，不把已识别天气处理声称为任意脚本天气已穷尽。
- 完成 11,578 个 HDR 面和 14,079 个 detail props。VRAD 改变 7 个叶节点的 SKY/SKY2D 标志，因此原始报告 `protected_lumps_unchanged=false`；严格审计确认叶 lump 仅这些允许的位变化，叶几何、面拓扑、sprp 和 detail 布置保持。不能把此结果写成所有受保护 lump 字节完全不变。compiler_diagnostics 为空。
- 单次自动采样、重载和正常退出，37 份 HDR cubemap 全部更新，RGB 有限且本轮没有非有限 alpha 文件；非 PAK 内容及采样坐标记录保持，LDR/default 保留。95 项临时路径清理完成，启动器 INI 原哈希恢复。141 份原始内嵌 VMT、所选 NAV/exclude 字节保持。

最终包八个条目经原生解包及哈希检查，SHA256 `3c3f1278983902dc5264b7908339aa4c38332b75bcf72b5f031f7014c17b3217`。先确认游戏关闭与覆盖冲突，再可逆停用旧 C4 材质实验包 `0b6ed222f24e47b2`，部署新 VPK 和 `lmc_c4_c7_validate` / `lmc_c4_c7_info`，安装文件及旧备份哈希复核通过。C2→C7 包及其他不同地图插件保持。

证据、部署收据及步骤在 `artifacts/c4-c7m1-20260918/`；截图建议 `C4M3-C7M1-stage-final.png`。目前机器状态 final_ready_pending_user_validation，等待用户观察天气、材质、受光和实际游玩。C7M1 预设已有 C2M1 人工通过，按用户计划在本轮 C4M3 反馈后再确认第二样本阶段成果。原材质光泽 VIS-003 保持暂缓，不因本轮天气覆盖而宣称解决。

### C7M1 当前阶段接受，开始 C10M3 夜景

用户随后确认“没有问题，C7预设我认为当前阶段可以验收了”。C4M3 最终包 `3c3f1278983902dc5264b7908339aa4c38332b75bcf72b5f031f7014c17b3217` 的接受记录保存在对应运行 `manual-validation/feedback-20260918-accepted/acceptance.json`，并同步录制资料库。未发现约定名称的新截图，不虚构图像附件。C7M1 以 C2M1/C4M3 两个已验收样本完成当前阶段，不扩大为任意输入、全路线或全部模式通过。

2026-09-18 至 19 日按用户要求准备 `c10m3-night-v1`，首个输入仍为原始 C2M1。参考 C10M3 BSP SHA256 `d2914994c57bb07d25c213f2f833aa92e2d266605c904f25e3e80762b04c6653`，预设 JSON SHA256 `499fbe534145dfc7de99bc0658109cbd6a62601f78861bc0c4fa62868ae5ebd7`。参数选择、规范化值和未迁移内容见 [C10M3 说明](c10m3-preset.zh-CN.md)。所选角色完整字段（含方向）及六条曝光初始化在基础和 h/l/s 中一致；没有参考地图专用转换分支，生产解析逻辑仅新增内置预设登记。

测试先验证新 ID 不支持及发行清单缺失时失败，再接入数据并验证通过。完整回归按 `python -m unittest discover -s tests` 执行 396 项，393 通过、3 项既有符号链接权限跳过。最初未指定 `-s tests` 的调用遇到既有测试间导入路径错误，纠正调用后完整通过，不修改程序掩盖测试问题。旧 C5M1/C4M3/C7M1 三套预设在八张实际输入及现存模式补丁上比较 81 组：72 组输出字节完全相同、9 组拒绝结果相同。

独立只读复核逐项对照参考数据和声音引用，未发现实质性问题；相关 9 项测试中 8 通过、1 项权限跳过。C2M1 预检查基础与 h/l/s 通过。9 项直接预设资源和 6 项所引用 PC HDR 天空纹理已读取并记录来源/哈希，未将低硬件等级 fallback 或任意 shader 依赖称为已全面覆盖。参考抽取、方案选择、回归和资源证据均在 `artifacts/c10m3-preset-20260918/`。此阶段尚未对 C10 目标作人工画面验收。

### C2M1→C10M3：独立工具包机器流程完成

2026-09-19，从本地源码提交 `2d95911` 打包后独立解压执行，源码 ZIP SHA256 `082ef5d6bcf132444a59b3b752aa290e701ff74f572004fb1821413fd4c8949c`；Windows 原生工具 ZIP 重新解压并核对 `c966088e05a9d8ea59f9e46b5f247b874c37e86c63f0f82d14518161a05550a8`。独立源码清单逐文件哈希通过，相关测试 39 项中 38 通过、1 项符号链接权限跳过；该子集同样需要将 tests 目录纳入测试模块搜索路径，未修改生产算法。

运行 `028c34159502430d`（`runs/c2m1-c10m3-v3-01`），输入与本机原始 C2M1 提取文件逐字节相同。generic-replace-v3、材质 preserve。基础及 h/l/s 的受保护实体/IO 通过；最终四个容器逐项核对所有预设角色与初始化曝光/Bloom，env_sun 已移除，额外方向光存在且参数匹配，现存风控制器按 v3 calm 值归一化。

VRAD 完成 5,764 个 HDR 面、38,443 个 detail props，面拓扑、叶几何、sprp、detail 布置和受保护 lump 保持；SKY/SKY2D 标志变化为 0，compiler_diagnostics 为空。独立代码目录自动启动正常测试启动器，单次 CAPTURE_SENT、重载、正常退出，63 份 HDR 反射全部更新。非 PAK 数据与采样坐标记录保持，LDR/default 保留；原生输出四份非有限 alpha 按既有策略报告并原样保留，所有存储 RGB 有限，未据此宣称视觉正确。采样请求后无拒绝记录。

最终 VPK 八个条目通过原生解包与逐文件哈希，SHA256 `06bfbc42cef8a4d4cf46a177aa775043f02647ddc24049f5b1fcb73a1f501948`。207 份原始内嵌 VMT 保持，NAV/exclude 与来源一致。147 项临时采样路径清理完成，启动器 INI 恢复原哈希。最终包和 offline 包已加入本地录制资料库，现共 33 份、1,167,957,351 字节，哈希核对无缺失或不符。

确认游戏关闭和同图冲突后，可逆停用原 C2→C7 包为 `.vpk.disabled-before-c10m3`，安装本轮最终 VPK 和 `lmc_c2_c10_validate` / `lmc_c2_c10_info`，部署文件及旧包备份再次哈希核对通过。原 C2→C4 备份、C4→C7 及其他不同地图插件保留。证据与手动步骤位于 `artifacts/c10m3-preset-20260918/`，建议截图 `C2M1-C10M3-stage-final.png`。状态 final_ready_pending_user_validation；尚未收到本轮画面反馈，合作模式之外的角色视角仍待单独验证。

### C2M1→C10M3 人工接受

2026-09-19 用户反馈“本次人工复核没有发现问题”，保存 `C2M1-C10M3-stage-final.png`。已查看汽车旅馆附近的夜景截图，并将截图、SHA256、用户反馈和最终包身份归档至 `runs/c2m1-c10m3-v3-01/manual-validation/feedback-20260919-accepted/`，同步本地 VPK 录制资料库。记录为实际观察范围接受，不扩大为全路线、全部模式或任意地图通过。用户指定下一张输入 C5M1，继续应用相同 C10M3 预设，不改变预设参数或转换算法。

### C5M1→C10M3：离线烘焙完成，等待游戏退出后采样

运行 `0e7c354f264a4dbc`，目录 `runs/c5m1-c10m3-v3-01`，使用与首轮一致的独立源码及原生工具包重新解压、逐文件验证，源码提交 `2d95911`，generic-replace-v3，材质 preserve。输入 SHA256 `80bdc1099e4ba4c7ba86b9281e3749d439e48f39d13424d490b71b4741e09dab` 与先前 C5M1 原始输入一致，不读取已部署的风格包作为来源。基础与实际存在的 h/l 模式补丁受保护实体/IO 检查通过，没有 s 补丁。

VRAD 完成 6,362 个 HDR 面、2,096 个 detail props，面拓扑、叶几何、sprp、detail 布置及受保护 lump 保持，SKY/SKY2D 标志变化 0，compiler_diagnostics 为空。构建退出码 0，状态 offline_ready。离线 VPK 原生解包及七个条目检查通过，SHA256 `9ff830b678484f6d4773d9dcd6f875f125e0e96674b18f7c3153e54d42f117a2`；另存录制资料库，现 34 份、1,190,085,872 字节，归档逐包哈希核对通过。

检测到用户测试版游戏仍运行，已请求完全退出，因此此刻尚未启动本轮自动采样、生成最终包或部署。扫描 18 份 addon 目录 VPK 发现一份旧 C5→C4 覆盖包 `5a3aef3dc95f49d4`，哈希与既有接受包一致；最终部署时计划可逆备份停用，当前保持。证据、配置和预备手动指导在 `artifacts/c5-c10m3-20260919/`，指导需待最终部署收据成功后执行；离线包不能当作最终反射完成或人工验收通过。
