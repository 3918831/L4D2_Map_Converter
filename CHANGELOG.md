# 变更记录

## 未发布 — 通用输入与强预设转换

- 新增 batch-run：复用单图构建与自动采样，逐图核对输入/方案/最终包身份，保留日志与待验收清单；离线失败独立记录，采样/共享环境异常停止队列，不重试或自动部署最终包。

- 新增 batch-check：清单驱动串行单图预检查，独立失败继续、逐项检查点、阶段分类、配置/输入/程序身份与 JSON/Markdown 汇总；不构建、不采样、不自动接受。

- 新增显式 generic-replace-v2：按资源及事件关系识别天气音效/混音、雾插值、纯画面震动和天空雾命令；模板风实体归一化并保留生命周期。保护共享玩法调用、物理震动及未知脚本，输出天气证据和未覆盖项。
- v2 运行清单、配置和修改方案必须使用一致规则版本，防止采样阶段误用旧路径。v1 配置和旧来源适配不自动迁移。

- 新增只读 analyze、伴随文件发现和角色/IO 分析。
- 新增 generic-replace-v1 配置和确定性修改方案，按类型覆盖视觉控制器，保护无关实体/IO；混合目标、脚本和生命周期歧义拒绝并报告。
- 按实际 h/l/s 补丁集合构建，手动及自动采样使用本轮曝光控制器，保留旧地图适配路径。
- 新增通用使用指南和回归测试；真实地图验证范围逐项记录，不宣称所有输入已通过验收。

## 0.6.0 — 独立 Windows 原生工具包

- 新增固定八文件的原生工具 ZIP 打包脚本，保留原二进制，记录文件大小、x86 架构与 SHA256；检查输入变化、ZIP 内容和已有输出保护。
- 文档支持通过现有 `tools_dir` 使用解压后的独立工具包，完整游戏资源和 Python 仍为外部依赖；源码与原生二进制分别归档。
- 新增可选 `native_mounts: resource_roots`，在运行目录生成绝对资源挂载配置，解决原生工具搬离游戏目录后的 DLC 模型缺失；旧配置默认保留原 `gameinfo` 行为。
- 明确挂载模式精确审计 VRAD 重算的叶节点天空可见标记，继续保护叶几何、碰撞、分区、PVS 及其他标志；旧模式保持完整叶数据保护。

## 0.5.0 — 可选无注入自动 HDR 采样

- 新增 `auto-capture --run --launcher`：正常启动器 → 精确进程/参数核验 → 开场与材质等待 → 原生守卫与单次采样 → 重载审计 → 退出 → 归档回填/打包 → 清理。继续使用原有严格 BSP/PAK 审计，最终状态等待用户验收。
- 启动配置独立于转换配置。ColdClient INI 方式已实测，临时追加启动参数后按哈希恢复原内容；显式 executable 方式提供配置接口，Steam 正式版启动适配尚未实测。
- 新增持久化请求状态、进程识别、STOP 邮箱及 `recover-auto-capture`。异常结果不自动重试；清理先确认游戏退出、归档并逐文件验收收据，外部修改保留。
- 自动模式首批支持 `replace` 的 C2→C4 固定阴天和 C6→C5 晴天；保留人工 CFG 路径。不增加来源地图、雨雷、DLL 注入、Portal 依赖或自动玩法验收。
- 补充启动器模板、中文自动指南和失败恢复测试。预设 JSON 与已接受地图包不改写。

## 0.4.0 — C4M3 固定阴天预设与 C2 验收归档

- 新增 `c4m3-overcast-static-v1`，首批仅支持原始 C2M1 搭配 `replace`；采用 C4 初始化默认的天空、光照、雾、后处理和曝光，不新增雨、闪电、雷声或动态风暴。首轮 63 份 HDR 反射已采样、审计并回填原名；用户确认“没什么问题，C4风格化可验收”，验收限该次最终包及实际测试范围。
- 新增 schema 2，表达无太阳光斑、未设置 bright-pixels、max 10 曝光、普通风、局部氛围完整覆盖和干燥声音定义；C5 schema 1 JSON、历史转换路径与已有成果保留。
- C2 覆盖雾、后处理及主/开场/检查点调色，保留开场时序和玩法；声音采用 C4 非天气室内 room-tone，室外有意不加环境循环声，车辆警报等事件声音保留。
- 生成地图专属 soundscape 脚本，临时采样包含别名脚本，原名离线/最终包包含原名脚本；有 exclude 的 C4 包为八文件，C5 仍为七文件。发行包增加 C4 JSON 与使用指南，不包含 Valve 音频或整套关卡脚本。
- 提供独立 `examples/c2-c4.example.json`，要求原始 C2 输入和全新输出目录；测试只停用冲突的已挂载 C5 addon，不删除旧预设、配置或接受成果。

## 0.3.0 — 版本化 C5 预设与 C6 接受成果归档

- 内置 `c5m1-daylight-v1` JSON 预设，固定目标角色参数、曝光、资源引用及晴天气氛；预设不包含地图几何或游戏资产，转换不再要求提供 C5 BSP/LMP。
- 新配置用 `source_profile: c6m1_riverbank/c2m1_highway` 和 `preset: c5m1-daylight-v1`；保留 `profile` 加显式参考文件的旧格式，拒绝两套选择项混用。C6 默认 `replace`、可选 `preserve`；C2 保持已接受的 `preserve`。
- 预设经严格校验后提供转换角色视图，运行记录其版本、SHA256 和资源依赖；预设 JSON 随源码/Python 包分发。旧运行保持原配置和哈希语义，不自动改写。
- 记录 C6 晴天 run `49f6ed92538349a1` 的 48 张 HDR 反射导入、最终七文件包验证和用户“基本上没有什么问题”的接受反馈；不推断所有模式、全路线或画质均已测试。
- 文档明确来源适配、目标预设与执行工作流边界；来源仍限两张已适配地图，不新增任意 C7 支持、天气体积或 GUI。

## 0.2.0 — C6 晴天覆盖与采样重载交接

- C6 的 `atmosphere_policy` 默认改为 `replace`；`preserve` 可选择旧版保留天气行为。C2 已接受配置不变。
- 在基础 BSP 与 h/l/s 实体补丁中移除 34 个已核实天气专属实体、145 条天气相关输出。按 Hammer ID、实体类别/名称和控制签名识别；签名覆盖 `OutAnger`、模板与脚本字段，未知变体拒绝。新娘 Witch 尸潮保留，只移除风暴分支。
- 三个雾控制器统一为 C5 室外参数，三个后处理控制器统一为 C5 外景效果，检查点调色采用 C5 主调色。56 个雨声环境音区域改用已核实的 C5 水岸/商店定义，新增其资源存在性检查。
- 建筑、物件、局部灯光/材质、碰撞/NAV、原天气体积的底层 BSP 数据与无关玩法保留；不新增天气几何。原关卡加密脚本仍保留，未解释其代码，不宣称全流程已验收。
- 采样说明明确正常启动器传递进程参数 `-insecure`、核对进程和本地 `status`。加载设置 `sv_lan 1`；守卫新增 LAN 与已连接本地主机检查，不能代替人工 VAC 核对。
- 修复日志来源检查过宽：导入要求精确 run/别名的地图→guard→唯一请求顺序；区分采样后的重载/拒绝与首次请求失败。重复请求日志拒绝，进度结束仍需 BSP 审计。
- 完善旧运行归档/收据清理、新晴天配置和完整重启步骤，保留原中文双语交接。采样后重连拒绝不要求删除插件。
- 补齐原模型兼容修复的资源快照：MDL 校验值相同的缓存模型也追踪 VVD/VTX，避免伴随资源中途变化漏检。

## 0.1.1 — C6 静态模型光照缓存兼容修复

- 修复历史 BSP 光照缓存与当前 DLC/update 模型版本不同导致的 `VHV topology changed` 构建拒绝。只有当前 MDL/VVD/DX90 VTX 的校验值、顶点索引和有序光照网格布局共同支持时，才允许缓存版本迁移；未知变化继续拒绝。
- 构建前记录使用到的模型资源来源和 SHA256，烘焙后、打包后以及后续采样命令开始时重新检查，防止中途切换模型资源。
- 审计记录逐实例的模型、旧/新校验值和网格分段数量。地图几何、碰撞、导航、模型引用及布置、实体和原有 I/O 保护规则保持。
- 补充合成资源、真实校验流程和失败场景回归测试；更新 C6 构建失败后的重试说明。
- 此修复不包含此前讨论的强天气预设；C6 原有风暴行为仍保留。实机画面与流程验收仍由用户完成。

## 2026-09-13 — 稳定工作流基线

- 增加 JSON 配置驱动的检查、全质量 HDR 烘焙、结构审计和官方 VPK 打包/解包验证。
- 保留受限 C2M1→C5 配置，增加独立 C6M1→C5 配置；C6 保留风暴、雨和闪电事件，实机验收待用户完成。
- 增加临时别名采样准备、显式安装、游戏退出后归档、HDR 反射审计/原名回填及收据清理命令。
- 生成带唯一 run ID 的加载、检查、采样、恢复 CFG/VScript 和 `CAPTURE-STEPS.txt`；游戏由用户正常启动。
- 记录配置、输入/工具哈希、产物、状态和诊断；失败烘焙使用新输出目录，导入/打包可保留编号 attempt 后重试。
- 提供 C2/C6 示例、中文指南、架构与验证边界、源码发行说明及合成测试。

历史 18 号 C2 HDR 成果已通过当时用户验收，19 号确认重复应用已有配置不再改变基础 BSP 与三模式 LMP。这些结论不自动覆盖新生成包、C6、其他画质或模式。

本版为需另装 Python 3.11+ 的源码工具包，外部游戏资源和官方工具不随包提供。许可证与远程仓库由项目所有者决定。八阶段演示生成、自动启动采样、GUI/EXE 和其他地图适配留待后续。
